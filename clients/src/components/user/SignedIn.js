import React from 'react';
import { Text } from "react-native";

export default () => (
  <Text>you are already signed in. If you want to sign in as a different user, please sign out first.</Text>
);
